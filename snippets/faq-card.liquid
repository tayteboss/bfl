{% comment %}
  Renders a single FAQ accordion item

  Expected params from section:
  - question: string
  - answer: richtext
  - block: block object (for id + shopify_attributes)
{% endcomment %}

{% assign faq_uid = block.id | default: question | handleize %}

<button
  class="faq-card"
  type="button"
  aria-expanded="false"
  aria-controls="faq-content-{{ faq_uid }}"
  id="faq-header-{{ faq_uid }}"
  data-faq-trigger
  {{ block.shopify_attributes }}
>
  <div class="faq-card__indicator"></div>
  <p class="faq-card__question type-p">
    {{ question }}
  </p>
  <div
    class="faq-card__content"
    id="faq-content-{{ faq_uid }}"
    role="region"
    aria-labelledby="faq-header-{{ faq_uid }}"
    hidden
    data-faq-content
  >
    <div class="faq-card__inner">
      <p class="faq-answer type-p">
        {{ answer }}
      </p>
    </div>
  </div>
</button>
